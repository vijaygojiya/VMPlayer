import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  flContainer: {
    marginHorizontal: 8,
  },
  hamburgerIconStyle: {
    height: 30,
    width: 30,
  },
});
export default styles;
