import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  flContainer: {
    marginHorizontal: 8,
  },
  searchIconStyle: {
    height: 24,
    width: 24,
  },
});
export default styles;
